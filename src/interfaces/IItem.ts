export default interface IItem {
  supplierId: string;
  barcode: string;
  itemCode: string;
  itemName: string;
  category: string;
  stockNumber: number;
  itemType: string;
  supplier: string;
  reOrderLevel: string;
}
